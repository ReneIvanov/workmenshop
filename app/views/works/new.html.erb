<!-- View for a separate create of new work (e.g. by admin) --> 
<h2>Create a new Work</h2>

<br>

<%= form_with(model: @work, local: true, url: works_path) do |form| %>
  <% if @work.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work.errors.count, "error") %> prohibited this work from being saved:</h2>

      <ul>
      <% @work.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <div class="field">
   <%= form.label :title %>
   <%= form.text_field :title %>
 </div>

 <br>

  <div class="actions">
    <%= form.submit "Create work"%>
  </div>
<% end %>

